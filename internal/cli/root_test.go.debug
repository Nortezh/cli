package cli

import (
	"bytes"
	"fmt"
	"testing"
)

func TestDebug(t *testing.T) {
	cmd := NewRootCmd()
	var out bytes.Buffer
	cmd.SetOut(&out)
	cmd.SetArgs([]string{"--help"})
	cmd.Execute()
	fmt.Println("CAPTURED:", out.String())
}
